= render 'shared/login-header'
- breadcrumb :update_email_pass
= render 'layouts/breadcrumbs'
.wrapper
  = render 'mypages/mypage-sidebar2'
  .left-content
    %section.left-chapter-container
      %h2.left-chapter-head 
        メール/パスワード
      = form_for(current_user, local: true, url: user_path(current_user), html: {method: "patch"}) do |f|
        .left-single-content
          .top-form-group
            %label.change-mail メールアドレス
            = f.email_field :email, placeholder: "PC・携帯どちらでも可", class: "input-default"
            %p.form-info-text メールアドレスを変更すると確認メールが送信されます。メール内のURLをクリックすると変更完了です。
          .form-group
            %label.change-pass 現在のパスワード
            = f.password_field :password, placeholder: "", class: "input-default"
          .form-group
            %label.change-pass 新しいパスワード
            = f.password_field :password, placeholder: "7文字以上", class: "input-default"
          .form-group
            %label.change-pass 新しいパスワード確認
            = f.password_field :password, placeholder: "7文字以上", class: "input-default"
            %p.form-info-text パスワードを設定したい場合は上記をすべて入力してください。(設定しない場合は空欄)
          = f.submit "変更する", class: 'btn-default'
= render 'shared/exhibition-button'
= render 'shared/app-banner'
= render 'shared/footer'